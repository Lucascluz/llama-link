import Chat from "./components/chat";

function App() {
  return (
    <div className="p-4 bg-[var(--background)] text-[var(--foreground)] min-h-screen flex items-center justify-center">
      <Chat />
    </div>
  );
}

export default App;
